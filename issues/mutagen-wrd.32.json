{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "closed_at": "2026-05-15T01:20:50Z",
  "created": "2026-05-15T00:45:46Z",
  "description": "Spike output from mutagen-wrd.19. **Option B disposition.**\n\nAdvances: mutagen.coverage, mutagen.mutation_pipeline\n\n## Root cause (confirmed by mutagen-wrd.19 spike)\n\nEnd-to-end Scenario 7 (`:ecto_user_scenario`) has been `@tag :skip` since\n`.13` under the framing \"cover-instrumentation issue with Ecto-style\nDSLs\". That framing is wrong.\n\nDirect reproduction (`priv/helper_scripts/spike_19_repro.exs`, landed in\n.19) showed every macro-injected callback on `LaneFixture.EctoUser`\n(`__schema_kind__/0`, `field/2`-generated `name/0` / `age/0`, `birthday/1`,\nand the persisted `:lane_schema_kind` attribute) survives the full\n`:cover.compile_beam/1` → `:cover.stop/0` → `:code.purge/1` →\n`:code.load_file/1` cycle byte-for-byte. Module `__info__(:md5)` returns\nto the disk-baseline value after `:cover.stop/0`.\n\nThe baseline-red (`failed: 1, passed: 5`) comes from a single assertion\nin the FIXTURE test:\n\n```elixir\n# test/fixtures/lane_project/test/lane_fixture/ecto_user_test.exs:30\ntest \"persisted attribute survives compile (tight)\" do\n  attrs = LaneFixture.EctoUser.__info__(:attributes)\n  assert Keyword.has_key?(attrs, :lane_schema_kind)\n  assert :registered in Keyword.get_values(attrs, :lane_schema_kind)\nend\n```\n\n`Module.register_attribute(__MODULE__, :lane_schema_kind, persist: true)`\nwithout `accumulate: true` produces `attrs == [..., lane_schema_kind: [:registered]]`\n— the attribute value is itself a list (Erlang attribute format wraps it\nbefore serialising into the BEAM attributes chunk). `Keyword.get_values/2`\nthen returns `[[:registered]]`, and `:registered in [[:registered]]` is\n`false`.\n\nThe test fails identically before, during, and after `:cover.compile_beam/1`.\nThere is no `:cover` interaction — the fixture-test assertion is just\nwrong.\n\n## Fix\n\n1. Fix the assertion in\n   `test/fixtures/lane_project/test/lane_fixture/ecto_user_test.exs`:\n\n   ```elixir\n   # Either flatten:\n   assert :registered in List.flatten(Keyword.get_values(attrs, :lane_schema_kind))\n\n   # Or fetch the single value list:\n   assert Keyword.fetch!(attrs, :lane_schema_kind) == [:registered]\n   ```\n\n2. Un-skip Scenario 7 in `test/mutagen_ex/end_to_end_test.exs:789` by\n   removing `@tag :skip` and replace the now-stale framing block-comment\n   (lines 776-787) with a brief pointer to mutagen-wrd.19's findings.\n\n3. Confirm `mix test --only ecto_user_scenario` is green and the bytecode-\n   identical-MD5 check at the end of the scenario passes.\n\n4. Update README \"Known limitations\" item 5 to mark Scenario 7\n   un-skipped (the entry already references this ticket).\n\n5. CHANGELOG: move the .19 investigation note from \"Investigation notes\"\n   to \"Fixed\", referencing this ticket.\n\n## Allowed touches\n\n- `test/fixtures/lane_project/test/lane_fixture/ecto_user_test.exs` (the assertion fix)\n- `test/mutagen_ex/end_to_end_test.exs` (un-skip + comment update; minimal scope)\n- `README.md`, `CHANGELOG.md` (caveat language)\n\n## Out of Scope\n\n- `lib/` source. The production code path is correct — this is purely\n  a fixture-test bug.\n- Refactoring the `LaneFixture.EctoUserSchema` macro DSL. The macro is\n  fine; the test that probes it was just written incorrectly.\n\n## Verification\n\n- `mix test --only ecto_user_scenario` is green\n- The Spike-I bytecode-identical-restore MD5 check passes for `LaneFixture.EctoUser`\n- README + CHANGELOG reflect the un-skip",
  "id": "mutagen-wrd.32",
  "labels": [],
  "parent": "mutagen-wrd",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: 866c9970-ee52-41f7-b573-aa2ec26231c0\nLog: ~/.claude-personal/projects/-Users-mz-dev-mz-mutagen-ex/866c9970-ee52-41f7-b573-aa2ec26231c0.jsonl\n\nCommit: 9f18e6171fb2882be7e5b6d7b48342b91f18b810\nStage worktree: /Users/mz/dev/mz/mutagen_ex-ecto-user-unskip (branch mutagen-wrd.32/ecto-user-unskip, branched off epic/mutagen-wrd HEAD b2b8800)\n\nFiles touched:\n- test/fixtures/lane_project/test/lane_fixture/ecto_user_test.exs (the assertion fix per ticket fix step 1)\n- test/mutagen_ex/end_to_end_test.exs (un-skip Scenario 7 + replace stale framing per ticket fix step 2)\n- README.md (Known limitations item 5 marked resolved per ticket fix step 4)\n- CHANGELOG.md (moved .19 investigation note to new \"Fixed\" section per ticket fix step 5)\n- .spec/decisions/timeout_handling.md (correct disproved framing per orchestrator's worker prompt)\n\nTests added/changed:\n- LaneFixture.EctoUserTest \"persisted attribute survives compile (tight)\" — assertion fixed\n  (List.flatten/Keyword.fetch! both asserted; comment cross-references mutagen-wrd.19/.32)\n- MutagenEx.EndToEndTest Scenario 7 \"after full mutation pass, source and bytecode are\n  identical (r6, r11)\" — `@tag :skip` removed; bytecode-identical-MD5 invariant now\n  exercised end-to-end\n\nVerification stubs flipped: none (this ticket has no spec-verification stubs; it's a\nfixture-test fix + un-skip + doc cleanup advancing mutagen.coverage and\nmutagen.mutation_pipeline by exercising the existing r6/r11 contracts against the\nEctoUser DSL fixture).\n\nAudit-expanded scope:\n- `.spec/decisions/timeout_handling.md` — outside the ticket's Allowed touches\n  (test/, README.md, CHANGELOG.md). Edited per the orchestrator's worker-prompt\n  directive: \"Also correct the disproved framing in .spec/decisions/timeout_handling.md\n  per .19's spike findings.\" The Consequences -\u003e Positive section originally claimed\n  the Code.Server load-lock release unblocks both Scenario 4 (`infinite_looper.ex`)\n  AND Scenario 7 (`EctoUser` bytecode-identical restore). The .19 spike disproved\n  the Scenario 7 half — Scenario 7 is unblocked by the fixture-test assertion fix,\n  not by this decision. The Scenario 4 attribution and the two-phase cancel +\n  `:code.purge/1` settle rationale are unchanged.\n\nDeferred must requirements: none.\n\nVerification ran in stage worktree:\n- `mix compile --warnings-as-errors`: clean\n- `mix test --only ecto_user_scenario`: 1 test, 0 failures\n  (Scenario 7 passes; post-pipeline `assert post_md5 == pre_md5` at\n  end_to_end_test.exs:819-821 passes)\n- `mix test` (default suite, e2e_slow excluded): 362 tests, 0 failures (13 excluded)\n\nNote (informational, not blocking): `mix test --include e2e_slow` shows a\npre-existing Scenario 4 (`infinite_looper_scenario`) golden drift in\n`test/mutagen_ex/golden/end_to_end_scenario_4_infinite_looper.json`. Confirmed\npresent before any change in this ticket (stash test). Not related to .32 — flagging\nfor the orchestrator's awareness.\n\nSummary: Fixed the list-of-lists membership bug in the fixture-test assertion\n(`Module.register_attribute/3` with `persist: true` wraps values in a list before\nserialising into the BEAM attributes chunk, so `Keyword.get_values/2` returns\n`[[:registered]]`). Removed `@tag :skip` from end-to-end Scenario 7 so the\nSpike-I bytecode-identical-restore invariant is now exercised against the\nhand-rolled DSL. Updated README, CHANGELOG, and the disproved-framing portion of\n.spec/decisions/timeout_handling.md to match the .19 spike's findings.",
      "timestamp": "2026-05-15T01:10:25Z"
    },
    {
      "text": "VERIFY: PASS\nSession: 866c9970-ee52-41f7-b573-aa2ec26231c0 (verifier subagent)\nLog: ~/.claude-personal/projects/-Users-mz-dev-mz-mutagen-ex/866c9970-ee52-41f7-b573-aa2ec26231c0/subagents/agent-verify-mutagen-wrd-32.jsonl\n\nMechanical checks:\n- Files touched: 5\n  - test/fixtures/lane_project/test/lane_fixture/ecto_user_test.exs (Allowed)\n  - test/mutagen_ex/end_to_end_test.exs (Allowed)\n  - README.md (Allowed)\n  - CHANGELOG.md (Allowed)\n  - .spec/decisions/timeout_handling.md (Audit-expanded scope — see below)\n- Files outside Allowed globs (after expansion): 0\n- Files matching Out-of-Scope globs: 0 (no lib/ touches; no LaneFixture.EctoUserSchema macro DSL refactor)\n\nAudit-expanded scope:\n- .spec/decisions/timeout_handling.md is outside .32's literal Allowed touches (test/, README.md, CHANGELOG.md). Authorized by the orchestrator's worker prompt: \"Also correct the disproved framing in .spec/decisions/timeout_handling.md per .19's spike findings.\" Cross-references mutagen-wrd.19's verifier note that the .spec/decisions/ framing correction was appropriately deferred from .19 (whose Allowed touches did not include .spec/) to .32, where it pairs naturally with the assertion-fix + un-skip. The diff narrows the Consequences -\u003e Positive paragraph to attribute Scenario 4 to the load-lock-release rationale only, and re-attributes Scenario 7 to .32's fixture-test assertion fix per the .19 spike's disproved framing. Scenario 4 attribution and the two-phase cancel + :code.purge/1 settle rationale are untouched.\n\nSemantic checks:\n- Spec stubs run (Verification section of the ticket):\n  - mix test --only ecto_user_scenario: 1 test, 0 failures (374 excluded). The outer driver assertion at end_to_end_test.exs:815-817 (post_md5 == pre_md5) passes; the bytecode-identical-restore invariant is exercised end-to-end against LaneFixture.EctoUser through the full :cover.compile_beam -\u003e :cover.stop -\u003e :code.purge -\u003e :code.load_file cycle (the embedded ExUnit failure lines in stdout are the mutated test runs that SHOULD fail — these are kills the runner classifies, not driver failures).\n  - Spike-I bytecode-identical-MD5 check passes for LaneFixture.EctoUser (no `module MD5 changed after mutation pipeline` failure).\n\n- Source-claim tracing:\n  - The fixture-test assertion fix uses BOTH `List.flatten/1` on `Keyword.get_values/2` AND `Keyword.fetch!(attrs, :lane_schema_kind) == [:registered]` for tightness. Comment at ecto_user_test.exs:33-38 cross-references the .19 spike and .32. PASS.\n  - The fixture's `Module.register_attribute(__MODULE__, :lane_schema_kind, persist: true)` (no `accumulate: true`) is at test/fixtures/lane_project/lib/lane_fixture/ecto_user.ex:15. Confirmed.\n  - Standalone repro `priv/helper_scripts/spike_19_attrs.exs` re-ran cleanly: attrs = [vsn: [...], foo: [:bar]]; Keyword.get_values(attrs, :foo) = [[:bar]]; `:bar in [[:bar]]` = false; `:bar in List.flatten(...)` = true. The list-of-lists root cause claim is mechanically verified.\n  - end_to_end_test.exs Scenario 7 calls `run_pipeline!` which invokes the production CoverageRunner (lib/mutagen_ex/coverage_runner.ex calls :cover.compile_beam/1 at line 246-273). The driver loads LaneFixture.EctoUser, snapshots pre_md5 + pre_attrs, runs the full pipeline against the EctoUser scope, then asserts post_md5 == pre_md5 + attribute signature equality + macro-injected callbacks restored. The :cover.compile_beam lifecycle is genuinely exercised against the macro-heavy DSL.\n\n- For each must requirement Scenario 7 covers:\n  - mutagen.coverage.r3 (after cover, :code.which/1 returns a non-cover_compiled value): If broken, the driver's post-pipeline `apply(module, :__schema_kind__, [])` / `apply(module, :name, [])` / `apply(module, :age, [])` calls would either fail on a stale cover-compiled module pointer or return the wrong cover-instrumented bytecode's results.\n  - mutagen.coverage.r6 / .r7 (AST cache byte-identical source; no disk writes): If broken, the post-pipeline `assert_lane_tree_unmodified!(ctx.pre_hashes)` at line 826 would fail (lane fixture tree disk hashes would drift).\n  - mutagen.mutation_pipeline.r6 (after each mutation, Code.compile_quoted/1 restores bytecode-identical to AST cache): The load-bearing assertion. If broken, `post_md5 == pre_md5` at line 815-817 fails — the macro-injected `__schema_kind__/0` BIF table, the `field/2`-generated `name/0` / `age/0` BIF table, the `birthday/1` arithmetic, AND the persisted `:lane_schema_kind` attribute chunk all have to round-trip to the same MD5. Any drift in the Beam's attribute-chunk serialisation, or any compile-time side effect of `use LaneFixture.EctoUserSchema` not being re-run on restore, would surface here.\n  - mutagen.mutation_pipeline.r11 (no disk writes): Same disk-hash safety net at line 826.\n\n- Intent-level scope concerns: none.\n  - No lib/ touched.\n  - No LaneFixture.EctoUserSchema macro DSL refactor — the macro at test/fixtures/lane_project/lib/lane_fixture/ecto_user.ex:1-34 is untouched.\n  - Diff stays within fixture-test assertion fix + Scenario 7 un-skip + caveat-language updates + orchestrator-authorized framing correction.\n\n- Project verification:\n  - mix compile --warnings-as-errors: clean (exit 0)\n  - mix test: 362 tests, 0 failures (13 excluded) (exit 0)\n\nVerdict: PASS\n\nNotes (advisory, not blocking):\n- Pre-existing `test/mutagen_ex/golden/end_to_end_scenario_4_infinite_looper.json` drift under `mix test --include e2e_slow` flagged by implementer. Confirmed not caused by .32: the golden file was last touched at `4f7ee43` (mutagen-wrd.16) and .32's diff contains no infinite_looper code, no scenario 4 changes, and no golden-file edits. Pre-existing.\n- The audit-expanded `.spec/decisions/timeout_handling.md` edit is faithful to the .19 spike's findings and was correctly deferred from .19's verifier (`.spec/decisions/` not on .19's Allowed list; the framing-correction-pairs-with-assertion-fix routing is the natural seam). The two-phase cancel + :code.purge/1 settle rationale for Scenario 4 (which IS attributable to the load-lock release) is preserved verbatim.\n- Implementer's IMPLEMENTED comment correctly disclosed the orchestrator-authorized scope expansion.",
      "timestamp": "2026-05-15T01:14:44Z"
    },
    {
      "text": "AUDIT: APPROVE\nSession: ae2841e69da8d2deb (auditor subagent under parent 866c9970-ee52-41f7-b573-aa2ec26231c0)\nLog: ~/.claude-personal/projects/-Users-mz-dev-mz-mutagen-ex/866c9970-ee52-41f7-b573-aa2ec26231c0/subagents/agent-ae2841e69da8d2deb.jsonl\n\nReasoning: Each must-requirement Scenario 7 covers is exercised by an assertion that would fail if the requirement were violated. r6 lands on `assert post_md5 == pre_md5` at end_to_end_test.exs:815-817 (the macro-injected `__schema_kind__/0`, `field/2`-generated `name/0`/`age/0`, `birthday/1`, and the persisted `:lane_schema_kind` attribute chunk all contribute to the MD5 — any drift in the BEAM serialiser or any `use LaneFixture.EctoUserSchema` side-effect not re-run on restore surfaces here). r7 and mutation_pipeline.r11 are caught by `assert_lane_tree_unmodified!(ctx.pre_hashes)` at line 826 (SHA-256 per-file diff against pre-pipeline hashes computed at setup 189-193). r3 is exercised in two places: the pre-pipeline `:code.which/1`-is-`is_list` precondition at 788-794 plus the post-pipeline `apply(module, :__schema_kind__, [])` / `:name` / `:age` calls at 822-824 — any lingering cover_compiled state would surface in the apply-call results or the MD5 mismatch.\n\nSource-claim trace: standalone `priv/helper_scripts/spike_19_attrs.exs` re-run cleanly. `Module.register_attribute(__MODULE__, :foo, persist: true)` (no `accumulate: true`) at SpikeSchema.__using__ produces `Keyword.get_values(attrs, :foo) = [[:bar]]`; `:bar in [[:bar]]` is false; `:bar in List.flatten(...)` is true. Fixture's matching call at test/fixtures/lane_project/lib/lane_fixture/ecto_user.ex:15 has the same shape, so the assertion fix (`List.flatten` on `Keyword.get_values` + `Keyword.fetch! == [:registered]`) is correct for THAT shape — not a different shape.\n\nMechanical scope re-checked against base b2b8800: 5 files touched (test/fixtures/lane_project/test/lane_fixture/ecto_user_test.exs, test/mutagen_ex/end_to_end_test.exs, README.md, CHANGELOG.md, .spec/decisions/timeout_handling.md). 0 lib/ touches (`git diff -- lib/` empty), 0 fixture-lib touches (`git diff -- test/fixtures/lane_project/lib/` empty — the `LaneFixture.EctoUserSchema` macro at ecto_user.ex:1-34 is untouched), no macro DSL refactor. Verifier's count of 5 matches.\n\nOrchestrator-authorized scope expansion (.spec/decisions/timeout_handling.md) is sound: the .19 verifier note at mutagen-wrd.19's 2026-05-15T00:52:11Z comment explicitly observed the disproved framing was out of .19's Allowed touches and would land naturally in .32 paired with the assertion fix. The edit narrows the Consequences→Positive paragraph: removes Scenario 7 from the load-lock-release rationale, explicitly re-attributes Scenario 7 to .32's fixture-test fix per the .19 spike, and preserves verbatim the Scenario 4 (`infinite_looper.ex` → `:timeout`) attribution and the two-phase cancel + `:code.purge/1` settle rationale. Exactly what the orchestrator's directive called for.\n\n`mix test --only ecto_user_scenario` re-run from the stage worktree: 1 test, 0 failures (374 excluded). The embedded ExUnit failure lines in stdout are mutated test runs the pipeline classifies as kills, not driver failures — they don't fail the outer `assert post_md5 == pre_md5` or `assert_lane_tree_unmodified!`.\n\nApprove; do not close.",
      "timestamp": "2026-05-15T01:18:53Z"
    },
    {
      "text": "APPROVED. Merged into epic/mutagen-wrd at 2073466. Closing per orchestrator sign-off (auditor APPROVE on iteration 1).",
      "timestamp": "2026-05-15T01:20:50Z"
    }
  ],
  "priority": 2,
  "status": "closed",
  "title": "FIX: Scenario 7 (:ecto_user_scenario) un-skip — fixture-test persisted-attribute assertion bug",
  "type": "bug",
  "updated_at": "2026-05-15T01:20:50Z"
}
