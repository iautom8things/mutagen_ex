{
  "assignee": "Manuel Zubieta",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-05-13T23:07:46Z",
  "description": "Advances: mutagen.test_selection\n\nDiscovered during S7 (mutagen-wrd.9) end-to-end test driver authoring.\n\n## Problem\n\n`lib/mutagen_ex/test_selector.ex` at lines 46, 130, 142, 259 — `TestSelector.resolve/2` produces a `%MutagenEx.TestSelector.TestFilter{}` whose default `exclude` field is `[:test]` for file-cited targets (i.e. `mix mutagen --tests path/to/test.exs`). When this filter reaches ExUnit's tag rule, it excludes every test (since `use ExUnit.Case` stamps each test with the implicit `:test` tag) — net effect is the baseline / coverage / mutation passes see zero tests, classifying every mutation as `:survived`.\n\n## Reproduction\n\nRun the production `mix mutagen --scope lib/foo.ex --tests test/foo_test.exs` against any module: every mutation lands in the `survived` bucket because the cited test file never gets to run.\n\n## E2E driver workaround\n\nThe S7 end-to-end test driver at `test/mutagen_ex/end_to_end_test.exs` (`tests_collaborator/2`) intercepts file-cited targets and replaces the production filter with `%TestFilter{include: [], exclude: [], files: [target]}`. This is a TEST-ONLY fork; the production caller still hits the buggy default.\n\n## Resolution\n\nFix in `lib/mutagen_ex/test_selector.ex` so file-cited targets produce `exclude: []` (or whatever the spec resolves to after clarification). Retire the `tests_collaborator` fork in `end_to_end_test.exs` once the production code is fixed.\n\n## Related\n\n- mutagen-wrd.9 (S7 end-to-end suite, where this was discovered)",
  "id": "mutagen-wrd.11",
  "labels": [],
  "parent": "mutagen-wrd",
  "comments": [
    {
      "text": "IMPLEMENTED\nSession: eb14f76b-662e-400e-ba6b-22e2f3772aea\nLog: ~/.claude-personal/projects/-Users-mz-dev-mz-mutagen-ex/eb14f76b-662e-400e-ba6b-22e2f3772aea.jsonl\n\nCommit: 258665a\nStage worktree: /Users/mz/dev/mz/mutagen_ex-test-selector-exclude-fix\nStage branch: mutagen-wrd.11/test-selector-exclude-fix (based on epic/mutagen-wrd @ ca1c2b6)\n\nFiles touched:\n- .spec/specs/test_selection.spec.md  — r1 contract corrected (exclude=[]\n  for bare-file targets); r2/r3 spell out why their [:test] exclude is\n  load-bearing; r6 names the union rule; new scenario s6 covers the\n  file+tag union case.\n- lib/mutagen_ex/test_selector.ex  — TestFilter default + seed accumulator\n  + resolve_file/1 now emit exclude=[]. merge/3 takes a seed? flag and\n  uses set-intersection-ish union ([] wins) so bare-file contributors\n  broaden the filter rather than being suppressed by tag-cited siblings.\n- test/mutagen_ex/test_selector_test.exs  — updated s1 + union assertions\n  to expect the new shape; added regression cases for each target-shape's\n  exclude semantics (mutagen-wrd.11 regression guard); added a tag+tag\n  union case that keeps [:test] (no bare-file contributor present).\n- test/mutagen_ex/end_to_end_test.exs  — retired the tests_collaborator\n  fork; dispatch table now points at MutagenEx.TestSelector.resolve/2\n  directly. The e2e goldens were already written assuming exclude=[], so\n  no golden churn was needed.\n\nTests added:\n- MutagenEx.TestSelectorTest \"bare-file target sets exclude=[] (mutagen-wrd.11)\"\n- MutagenEx.TestSelectorTest \"file:line target sets exclude=[:test]\"\n- MutagenEx.TestSelectorTest \"tag target sets exclude=[:test]\"\n- MutagenEx.TestSelectorTest \"tag + tag targets keep restrictive exclude=[:test]\"\n- MutagenEx.TestSelectorTest \"file + file targets union into files list with exclude=[]\"\n- Updated MutagenEx.TestSelectorTest \"file-only target returns include=[], exclude=[], files=[path]\"\n  with explicit mutagen-wrd.11 regression comment.\n\nVerification stubs flipped: none new (v1/v2/v3 were already execute: true at\nend of mutagen-wrd.4 / refined by mutagen-wrd.9; this commit keeps them\ngreen and adds in-suite regression guards for r1's corrected contract).\n\nSpec edits: r1, r2, r3, r6 statements + scenarios s1 and s5 amended; new\nscenario s6 added. Spec changes describe observable behavior (filter\nshapes) — they do not weaken any invariant; they correct r1's literal\ncontract from a shape that excluded every test to one that admits them.\nThat is a strengthening, not a weakening (the prior wording could not\nhave been satisfied by any working implementation).\n\nSummary: TestSelector.resolve/2 now emits `exclude: []` for bare-file\n`--tests` targets, fixing the production bug where ExUnit's \"include +\nexclude :test\" pattern collapsed the test set to zero tests for any\n`mix mutagen --tests path/to/test.exs` invocation. File:line and tag\ntargets keep their existing restrictive `[:test]` exclude — these only\nwork because their `include` is non-empty. The end-to-end suite's\ntest-only workaround (`tests_collaborator`) is gone; the dispatch table\nnow exercises the production resolver directly, so the e2e suite is\nhonestly testing the production wiring it was masking before.\n\nLocal verification:\n- mix compile --warnings-as-errors  → clean\n- mix test                           → 308 tests, 0 failures (13 e2e_slow excluded)\n- mix test --only e2e_slow           → 13 tests, 11 pass, 2 honest skips (309 excluded)",
      "timestamp": "2026-05-14T10:14:53Z"
    },
    {
      "text": "VERIFY: PASS\nSession: agent-ae07c5594fb4c2c44 (parent: eb14f76b-662e-400e-ba6b-22e2f3772aea)\nLog: ~/.claude-personal/projects/-Users-mz-dev-mz-mutagen-ex/eb14f76b-662e-400e-ba6b-22e2f3772aea/subagents/agent-ae07c5594fb4c2c44.jsonl\n\nMechanical checks:\n- Files touched: 4\n  - .spec/specs/test_selection.spec.md\n  - lib/mutagen_ex/test_selector.ex\n  - test/mutagen_ex/end_to_end_test.exs\n  - test/mutagen_ex/test_selector_test.exs\n- Files outside Allowed globs: 0\n  Ticket lacks explicit \"Allowed touches\" frontmatter; the Resolution section\n  explicitly names lib/mutagen_ex/test_selector.ex and test/mutagen_ex/end_to_end_test.exs\n  (retiring tests_collaborator). The spec edit aligns r1's contract to the new\n  exclude semantics (Resolution: \"or whatever the spec resolves to after\n  clarification\"). The unit-test file is the natural co-edit for the lib change\n  and is where v1 + v3 stubs live. All four touches are within the bug's\n  resolution scope.\n- Files matching Out-of-Scope globs: 0\n  (Ticket has no Out-of-Scope (files) section.)\n\nSemantic checks:\n- Spec stubs run: 3, passed: 3\n  - v1 (mix test test/mutagen_ex/test_selector_test.exs) → 24 tests, 0 failures\n  - v2 (source_file test/mutagen_ex/test_selector_test.exs) → structural-source\n    check inside the \"tag resolution does not load any test module\" test —\n    runs as part of v1, passes\n  - v3 (mix test ... --only no_match_cases) → 4 tests, 0 failures\n- Requirements coverage (one line per must requirement):\n  - r1 (bare-file → include=[], exclude=[], files=[path]): covered by\n    test/mutagen_ex/test_selector_test.exs:68-79 (\"file-only target returns\n    include=[], exclude=[], files=[path]\") and :338-343 (\"bare-file target sets\n    exclude=[]\"). Would fail if resolve_file/1 reverted to exclude=[:test]\n    (the regressed shape that caused mutagen-wrd.11) — the exclude=[] equality\n    assert is direct and exact.\n  - r2 (file:line → include=[{:location, ...}], exclude=[:test], files=[path]):\n    covered by test/mutagen_ex/test_selector_test.exs:118-126 + :345-361.\n    Would fail if resolve_file_line/1 dropped the :location include or stopped\n    emitting exclude=[:test].\n  - r3 (tag:NAME → include=[name], exclude=[:test], files=walk): covered by\n    :143-185 (tag resolution + recursion + keyword-form variants) and :363-366\n    (exclude=[:test] regression guard). Would fail if resolve_tag/1 emitted\n    exclude=[] or omitted the tag from include.\n  - r4 (no module load; AST-walk only): covered by :198-227 — refutes presence\n    of Code.require_file / Code.eval_file / Code.eval_string / Code.compile_*\n    / ExUnit.Filters.parse_paths in the source AND asserts Code.string_to_quoted\n    is referenced. Would fail if anyone added a Code.require_file/eval call.\n  - r5 (zero-match → :no_tests_match error): covered by :234-273 — four\n    no-match cases (unused tag, empty test_root, missing test_root, file:line\n    outside any test block). Would fail if resolve_tag/resolve_file_line\n    returned an empty filter instead of {:error, %{reason: :no_tests_match}}.\n  - r6 (union: exclude=[] if any contributor is bare-file, else [:test]):\n    covered by :281-293 (file+file → exclude=[]), :295-310 (file+tag →\n    exclude=[]), :312-325 (tag+tag → exclude=[:test]). Would fail if\n    merge_exclude/3 lost its \"[] wins\" rule or stopped seeding from the first\n    contributor outright.\n- Intent-level scope concerns: none\n  - No new external dependencies (mix.exs / mix.lock unchanged).\n  - The e2e collaborator forks named `tests_collaborator` are retired; the\n    dispatch table now points at MutagenEx.TestSelector.resolve/2 directly,\n    matching the ticket's Resolution clause.\n  - r4 invariant (no module load) is reinforced — the new code path uses\n    Code.string_to_quoted only.\n- Project verification:\n  - mix compile --warnings-as-errors → clean (exit 0)\n  - mix test → 1 doctest, 308 tests, 0 failures (13 e2e_slow excluded)\n  - mix test --only e2e_slow → 13 tests, 0 failures, 2 skipped (matches\n    implementer's claim; the two skips are honest skips for mutagen-wrd.13\n    and mutagen-wrd.15 deferred items)\n- Audit-expanded scope (per step 11): none — no prior audit comment.\n\nVerdict: PASS\n\nNotes:\n- Spec correctness traced end-to-end: the merge_exclude/3 fast path\n  ([] wins after the seed contributor) implements r6's \"if any contributor\n  was a bare-file\" rule exactly, validated by case-split tests for all\n  four union shapes (file+file, file+tag, tag+file via dedup-order, tag+tag).\n- The end_to_end_test.exs change is a real production-coupling improvement,\n  not just a test cleanup: the dispatch table now feeds MutagenEx.TestSelector.resolve/2\n  unchanged, so the e2e suite would catch any future regression of r1 — which\n  is precisely what the original bug ticket wanted (the test-only workaround\n  was masking the production bug).\n- baseline_collaborator and coverage_collaborator still wrap the production\n  Baseline.run / CoverageRunner.run with a register_test_modules_for_phase!\n  preamble that calls Code.compile_file/1; these TODOs are tagged\n  bw mutagen-wrd.11 in source but the comments themselves explain why they\n  remain (Code.require_file caching across scenarios, not the TestSelector\n  exclude bug). Those forks are independent of this ticket's contract — the\n  exclude=[] fix is necessary but not sufficient to retire them. This is\n  advisory / paper-trail only and does not change the verdict.",
      "timestamp": "2026-05-14T10:20:15Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "DISCOVERY: TestSelector emits exclude: [:test] for file-cited targets",
  "type": "bug",
  "updated_at": "2026-05-14T10:20:15Z"
}
